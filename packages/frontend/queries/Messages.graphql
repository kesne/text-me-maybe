query Messages($threadID: Int!) {
    thread(threadID: $threadID) {
        id
        name
        recipient
        number
        createdAt
        ended
        messages {
            id
            body
            sender
            createdAt
        }
    }
}
