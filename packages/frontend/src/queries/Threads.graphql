query Threads {
    threads {
        id
        name
        recipient
        phoneNumber
        updatedAt
        ended
        lastMessage {
            id
            body
            seen
        }
    }
}
