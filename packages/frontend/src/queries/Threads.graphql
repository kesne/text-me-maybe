query Threads {
    threads {
        id
        name
        recipient
        number
        updatedAt
        ended
        lastMessage {
            id
            body
            seen
        }
    }
}
