query Messages($threadID: Int!) {
    thread(threadID: $threadID) {
        id
        name
        recipient
        phoneNumber
        createdAt
        messages {
            id
            body
            sender
            createdAt
        }
    }
}
