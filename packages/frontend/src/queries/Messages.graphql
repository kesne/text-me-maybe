query Messages($threadID: Int!) {
    thread(threadID: $threadID) {
        id
        name
        recipient
        phoneNumber
        createdAt
        ended
        messages {
            id
            body
            sender
            createdAt
        }
    }
}
